# ============================================================================
# CORE
# ============================================================================
DEBUG=True
SECRET_KEY='django-insecure-ix-#dj)eo1vu6l%c**t&cw!53gyyfyic824tueuek(&1l3rj4i'
ALLOWED_HOSTS=*

# ============================================================================
# FRONTEND CACHE REVALIDATION
# This secret must match REVALIDATE_SECRET in frontend .env
# ============================================================================
REVALIDATE_SECRET=elarbol_secret_2026
FRONTEND_REVALIDATE_URL=http://localhost:3000/api/revalidate

# ============================================================================
# CORS / CSRF
# ============================================================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_ALL_ORIGINS=False

# ============================================================================
# COOKIES / LOGGING
# ============================================================================
FORCE_SECURE_COOKIES=False
ENABLE_FILE_LOGGING=True

# ============================================================================
# DATABASE (comment রাখলে SQLite fallback ব্যবহার হবে — local dev এ ঠিক আছে)
# Postgres দিয়ে টেস্ট করতে চাইলে নিচেরগুলো uncomment করো
# ============================================================================
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=postgres
# DB_USER=icommerce
# DB_PASSWORD=icommerce2026
# DB_HOST=127.0.0.1
# DB_PORT=5433

# ============================================================================
# REDIS / CELERY
# local এ Redis সার্ভার না থাকলে এইটা comment রাখো — Celery/Channel Layer
# তখন default fallback (in-memory / 127.0.0.1:6379) ব্যবহার করবে
# ============================================================================
# REDIS_URL=redis://127.0.0.1:6379/0

# ============================================================================
# EMAIL
# ============================================================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=dev.isslbd@gmail.com
EMAIL_HOST_PASSWORD=puprvkdyaozaixyb